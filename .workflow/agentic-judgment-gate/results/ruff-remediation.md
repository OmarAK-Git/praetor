# Gate remediation — ruff I001

Fixed import sort in tests/engine/test_engine_ids.py via `ruff check --fix` (introduced by Task 14 session_trace_hash test addition).

Re-run gate commands after this note.
