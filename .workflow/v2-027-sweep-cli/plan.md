# Workflow Plan — V2-027 Org-Config Sweep CLI

Goal: V2-027 — Org-config sweep CLI with proposed artifacts, non-zero exit on invalid inputs, preflight rejection, operator docs.

Verification: pytest tests/codification/ -q
