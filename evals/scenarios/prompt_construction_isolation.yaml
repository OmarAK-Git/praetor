schema_version: "1"
scenario_id: prompt_construction_isolation
description: Prompt excerpts exclude raw_source and enforce excerpt bounds.
runner: prompt_isolation
setup: {}
expectations:
  raw_source_excluded: true
  excerpt_max_chars: 200
