# Operational Guide

Generated by the ultimate-agentic-workflow skill. Keep this file under 60 lines; status and progress belong in the active plan or `.workflow/`.

## Build And Run

- Build: `python -m build`
- Run: `python -m <module>`

## Validation

- Test: `pytest`
- Lint: `ruff check .`
- Typecheck: `mypy .`

## Operational Notes

- Add durable repo-specific lessons here only when they change future agent behavior.
- Bounded autopilot loop: `/gsd-autopilot-loop` with queue at `.workflow/autopilot-queue.json`.
- Load sprint tasks: `/gsd-load-sprint v2-3` then `/gsd-autopilot-loop --stop-before-gate` (Composer chat).
- Sprint gate: fresh chat, Claude UI model, `/gsd-autopilot-loop --task-id <gate-item-id>`.
- Models (task items): implement `composer-2.5-fast`, verify `claude-opus-4-8-thinking-high` via subagents — set in queue `defaults`.
- V2 plan SSOT: `docs/proposals/v2_implementation_plan.md`.

## Dependency And Repo Setup

- `.codex` / `.claude` setup and dependent `git clone` work require explicit user approval before execution.
- Approval requests must include exact commands, write targets, network/data risks, and rollback.
- Prefer project-local setup over global changes unless the user asks for global config.

## Codebase Patterns

- Source: `src`
- Tests: `tests`
